---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";
import Highlight from "../components/Highlight.astro";
import { getCollection } from "astro:content";
import createSlug from "../lib/createSlug";

const hls = (await getCollection("highlights")).sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf()
);

const latest = hls.slice(0, 3);
---

<BaseLayout sideBarActiveItemID="home">
  <div class="pb-12 mt-5">
    <div class="mb-6">
      <div class="text-6xl font-bold mb-2">Hi, I'm Beleicia! 👋🏾</div>
      <div class="text-2xl">
        🗣️ pronounced: <span class="italic">/buh-lee-see-ah/</span>
      </div>
    </div>
    <div class="py-2">
      <text class="text-xl">
        <p class="my-3">
          I am a Computer Science Ph.D. candidate at Stanford University, where
          I am co-advised by James Landay and Michael Bernstein.
        </p>
        <p class="my-3">
          Within my work, I explore how design can reflect more pluralistic and
          diverse lived experiences. To do this, I typically center the
          experiences of Black communities within my research and generate
          outputs that can be adapted to and by other communities.
        </p>
        <p class="my-3">
          Along this journey, I have had the opportunity to conduct industry
          research at Adobe Research and Spotify. I have also completed my Master's at
          the University of Illinois Urbana-Champaign, where I was advised by Karrie Karahalios.
        </p>
      </text>
    </div>
    <!-- <div class="mt-8">
      <a
        class="btn"
        href="https://twitter.com/manuelernestog"
        target="_blank">Download my CV</a
      >
      <a
        href="https://github.com/manuelernestog/astrofy"
        target="_blank"
        class="btn btn-outline ml-5">
        Get This template
      </a>
    </div> -->
  </div>

  <div>
    <div class="text-3xl w-full font-bold mb-2">✨ Latest Updates</div>
  </div>

  {
    latest.map((hl) => (
        <Highlight date={hl.data.month}, desc={hl}/>
    ))
  }
</BaseLayout>
